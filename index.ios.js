/**
 * Weather Along Route App - iOS Entry Point
 * Displays weather information along a travel route
 * @flow
 */

import React from 'react';
import { AppRegistry } from 'react-native';
import WeatherAlongRoute from './App';

// Register the app
AppRegistry.registerComponent('test', () => WeatherAlongRoute);
